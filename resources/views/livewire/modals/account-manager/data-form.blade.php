<div>
@if ($isOpen)
    @php([$heading, $fields] = \App\Livewire\Modals\AccountManager\DataForm::FORMS[$detailType])
    <div class="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4" wire:click.self="close"><form wire:submit="save" class="max-h-[92vh] w-full max-w-xl overflow-y-auto rounded-xl bg-white shadow-2xl"><div class="flex items-center justify-between border-b border-slate-200 px-5 py-4"><h2 class="text-base font-extrabold uppercase text-slate-900">{{ $heading }}</h2><button type="button" wire:click="close" class="text-xl text-slate-400">×</button></div><div class="grid gap-4 p-5 sm:grid-cols-2">@foreach ($fields as $field)<label class="block text-xs font-bold text-slate-700 {{ in_array($field, ['Ghi chú', 'Địa chỉ', 'Địa chỉ thanh toán']) ? 'sm:col-span-2' : '' }}">{{ $field }}@if (! in_array($field, ['Ghi chú', 'Recovery Email', 'Recovery Phone', 'Routing / SWIFT', 'Ngày hết hạn'])) <span class="text-rose-600">*</span>@endif @if (in_array($field, ['Ghi chú', 'Địa chỉ', 'Địa chỉ thanh toán']))<textarea wire:model="data.{{ $loop->index }}" rows="3" class="mt-1.5 block w-full rounded-md border-slate-200 text-sm"></textarea>@else<input wire:model="data.{{ $loop->index }}" class="mt-1.5 block w-full rounded-md border-slate-200 text-sm" @if (str_contains($field, 'Password')) type="password" @endif @if (in_array($field, ['4 số đầu tài khoản', '4 số cuối tài khoản'], true)) inputmode="numeric" pattern="[0-9]{4}" maxlength="4" placeholder="0000" @endif>@endif @error('data.'.$loop->index)<span class="mt-1 block text-xs text-rose-600">{{ $message }}</span>@enderror</label>@endforeach @error('data')<p class="sm:col-span-2 text-xs text-rose-600">{{ $message }}</p>@enderror</div><div class="flex justify-end gap-2 border-t border-slate-100 px-5 py-4"><button type="button" wire:click="close" class="rounded-md border border-slate-200 px-4 py-2 text-xs font-bold">Hủy</button><button type="submit" class="rounded-md bg-blue-600 px-4 py-2 text-xs font-bold text-white">Lưu</button></div></form></div>
@endif
</div>
